<Window x:Class="Nocturne.NativeClient.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:viewmodels="clr-namespace:Nocturne.NativeClient.ViewModels.MainWindow"
        DataContext="{Binding ViewModel}" d:DataContext="{d:DesignInstance Type=viewmodels:MainWindowViewModel}"
        xmlns:local="clr-namespace:Nocturne.NativeClient" xmlns:pages="clr-namespace:Nocturne.NativeClient.Views.Pages"
        mc:Ignorable="d"
        Title="Nocturne client" Height="450" Width="800">
    <Grid>
        <Frame Grid.Row="1" NavigationUIVisibility="Hidden" Content="{Binding CurrentPage}">
  
        </Frame>
    </Grid>
</Window>
